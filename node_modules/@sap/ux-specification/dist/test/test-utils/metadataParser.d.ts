import { ServiceMetadata } from '../../src/index';
export declare enum ParseError {
    MissingVersion = "No valid EDMX version"
}
/**
 * Get the version from EDMX string
 * @param edmxString - EDMX as XML string
 */
export declare function getEdmxVersion(edmxString: string): string;
/**
 * Converts EDMX XML 2.0 or 4.0 as buffer to EDMSX JSON 4.0
 * @param edmx -  EDMX file content, either v2 or v4
 */
export declare function parseServiceMetadata(edmx: string): Promise<ServiceMetadata>;
/**
 * Reads the service metadata
 * @param path Path to the service metadata
 */
export declare function readServiceMetadata(path: string): Promise<ServiceMetadata>;
