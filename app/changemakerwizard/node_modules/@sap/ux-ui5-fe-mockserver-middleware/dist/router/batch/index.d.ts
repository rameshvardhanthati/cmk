import { RequestHandler } from 'express-serve-static-core';
import { ODataMetadata } from '../data/metadata';
import { DataAccess } from '../data/dataAccess';
import { ServerConfig } from '../../api';
/**
 * @param metadata
 * @param dataAccess
 * @param config
 */
export declare function batchRequestHandler(metadata: ODataMetadata, dataAccess: DataAccess, config: ServerConfig): RequestHandler;
