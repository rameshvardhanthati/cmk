import Router from 'router';
import { FileBasedServerConfig } from '../api';
/**
 * @param config
 * @param version
 */
export declare function catalogService(config: FileBasedServerConfig, version?: number): Router;
